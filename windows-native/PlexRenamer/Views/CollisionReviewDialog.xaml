<ui:FluentWindow x:Class="PlexRenamer.Views.CollisionReviewDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Resolve collisions"
                 Width="780"
                 Height="600"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Resolve collisions" />

        <StackPanel Grid.Row="1" Margin="0,16,0,12">
            <TextBlock x:Name="HeadlineText"
                       FontWeight="SemiBold"
                       FontSize="14"
                       TextWrapping="Wrap"
                       Margin="0,0,0,4" />
            <TextBlock TextWrapping="Wrap"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Text="For each collision, pick the source that should land at the target path. The other sources will be marked as Skip in the next preview." />
        </StackPanel>

        <ItemsControl x:Name="CollisionsItems"
                      Grid.Row="2"
                      AutomationProperties.Name="Collision list">
            <ItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  BorderThickness="1"
                                  BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="12,10">
                        <StackPanel>
                            <TextBlock Text="{Binding Target}"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Target}" />
                            <TextBlock Text="{Binding Reason}"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                       Margin="0,2,0,6" />
                            <ItemsControl ItemsSource="{Binding SourceChoices}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <RadioButton GroupName="{Binding GroupName}"
                                                     IsChecked="{Binding IsWinner, Mode=TwoWay}"
                                                     Margin="0,2"
                                                     ToolTip="{Binding SourcePath}">
                                            <TextBlock Text="{Binding SourcePath}"
                                                       TextTrimming="CharacterEllipsis" />
                                        </RadioButton>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <ui:Button x:Name="CancelButton"
                       Content="Cancel"
                       Appearance="Secondary"
                       Margin="0,0,8,0"
                       IsCancel="True"
                       Click="OnCancelClick" />
            <ui:Button x:Name="SkipAllButton"
                       Content="Skip all colliding sources"
                       Appearance="Secondary"
                       Margin="0,0,8,0"
                       Click="OnSkipAllClick" />
            <ui:Button x:Name="ApplyChoicesButton"
                       Content="Apply selections"
                       Appearance="Primary"
                       IsDefault="True"
                       Click="OnApplyChoicesClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
